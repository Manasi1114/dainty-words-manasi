---
const { title, author, date } = Astro.props;
---


<!doctype html>
<html>
  <head>
    <title>{title}</title>
    <meta charset="UTF-8" />
    <link rel="stylesheet" href="/styles/global.css" />
  </head>
  <body>
    <main class="poem-page">
      <h1>{title}</h1>
      <p class="meta">By {author} — {new Date(date).toDateString()}</p>
      <article class="poem">
        <slot />
        <!-- This is where the poem text goes -->
      </article>
    </main>
  </body>
</html>
